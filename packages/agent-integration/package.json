{
  "name": "@zigrivers/agent-integration",
  "version": "0.1.0",
  "private": true,
  "description": "Shared cross-CLI agent-integration core: canonical skill bodies + per-platform renderers (SKILL.md, AGENTS.md block, Cursor .mdc) for Scaffold and MMR.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "lint": "eslint src/",
    "type-check": "tsc -p tsconfig.check.json",
    "check": "npm run lint && npm run type-check && npm test"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "@types/node": "^22.13.0",
    "eslint": "^9.21.0",
    "typescript": "^5.8.2",
    "typescript-eslint": "^8.26.0",
    "vitest": "^3.0.7"
  }
}
