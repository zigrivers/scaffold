name: Knowledge Freshness Gates

# CI gates for freshness PRs (spec §A.5).
#
# Fires on every PR targeting `main` that touches `content/knowledge/**`. The
# cron in `knowledge-freshness-audit.yml` opens PRs from
# `knowledge-freshness/<entry>-<date>` branches; human-authored knowledge
# edits also flow through these gates because the paths filter matches both.
#
# Five gates, one job, separate steps so a failure surfaces the exact gate.
#
# F-004: the gate implementation is built from the PR's BASE ref (origin/main),
# not the PR HEAD. Otherwise a PR that touches `content/knowledge/**` could ALSO
# modify the gate code to bypass itself. We checkout TWO refs: base for the
# build, HEAD-of-PR for the content under test (read-only).

on:
  pull_request:
    # Default activity types are opened, synchronize, reopened. We add
    # `labeled` and `unlabeled` so the gate workflow re-runs when a
    # maintainer applies/removes the `override:anti-over-rewrite` label
    # (F-003 round-3). Without these, the label-based override is
    # unusable — applying the label wouldn't trigger a re-run.
    types: [opened, synchronize, reopened, labeled, unlabeled]
    branches: [main]
    paths:
      - 'content/knowledge/**'

# Cancel an in-flight run if the same PR pushes again — gates are deterministic
# and re-running on every push is wasteful.
concurrency:
  group: knowledge-freshness-gates-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  gates:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    steps:
      # F-004 round-1 mitigation: ideally we'd build the gate CLI from the
      # PR's BASE ref (origin/main) so a malicious PR can't modify the gate
      # code itself, then overlay the PR HEAD's `content/knowledge/` into
      # the base checkout. We are NOT doing that yet because the gate code
      # is itself introduced by this Phase 2 PR — building from base on the
      # bootstrap PR fails because base lacks the new subcommands. Once
      # Phase 2 lands on main, future-Phase-3 hardening can switch to
      # base-built (and overlay head's content/knowledge) without breaking
      # ANY workflow. Until then: gate code from HEAD, content from HEAD.
      # The F-004 risk window is the period during which an attacker PR
      # could modify gate code on the same PR that touches knowledge files;
      # mitigated in practice by mandatory PR review.
      - name: Checkout PR HEAD
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          path: base

      - name: Set working directory and Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '18.17'
          cache: 'npm'
          cache-dependency-path: base/package-lock.json

      - name: Install npm dependencies (base)
        working-directory: base
        run: npm ci

      - name: Build CLI (base)
        working-directory: base
        run: npm run build

      - name: Resolve changed knowledge entries + capture diff
        id: changed
        working-directory: base
        # F-001: write the file list to a JSON file and pass it via
        # `--files-from`. We do NOT interpolate filenames into bash, because
        # a PR could add a content/knowledge path containing shell
        # metacharacters that would `$()`-execute when interpolated.
        #
        # F-001 round-3: `--diff-filter=d` excludes deleted entries from the
        # changed-files list so subsequent gates don't try to read missing
        # paths and crash with ENOENT.
        run: |
          set -euo pipefail
          mapfile -t FILES < <(git diff --name-only --diff-filter=d origin/main...HEAD -- 'content/knowledge/**/*.md')
          if [ "${#FILES[@]}" -eq 0 ]; then
            echo "no_changes=1" >> "$GITHUB_OUTPUT"
            echo '[]' > "$GITHUB_WORKSPACE/changed-files.json"
            : > "$GITHUB_WORKSPACE/pr.diff"
          else
            # Emit a JSON array. The CLI validates each entry server-side.
            printf '%s\n' "${FILES[@]}" \
              | jq -R . \
              | jq -s . \
              > "$GITHUB_WORKSPACE/changed-files.json"
            git diff origin/main...HEAD -- 'content/knowledge/**/*.md' \
              > "$GITHUB_WORKSPACE/pr.diff"
          fi
          printf 'changed knowledge files:\n'
          printf '  %s\n' "${FILES[@]:-<none>}"
          echo "files_json=$GITHUB_WORKSPACE/changed-files.json" >> "$GITHUB_OUTPUT"
          echo "diff_path=$GITHUB_WORKSPACE/pr.diff" >> "$GITHUB_OUTPUT"

      # ---------- Gate 1: validator (BLOCKING) ----------
      # F-005: standardize on `node dist/index.js validate-knowledge` so the
      # gate workflow and the cron's inline gates invoke the same CLI surface.
      # `npm run build` has already run, so dist/ is current.
      - name: 'Gate 1 of 5 — knowledge frontmatter validator'
        if: steps.changed.outputs.no_changes != '1'
        working-directory: base
        run: node dist/index.js validate-knowledge

      # ---------- Gate 2: link-check (BLOCKING) ----------
      - name: 'Gate 2 of 5 — source link-check'
        if: steps.changed.outputs.no_changes != '1'
        working-directory: base
        env:
          FILES_JSON: ${{ steps.changed.outputs.files_json }}
        run: node dist/index.js knowledge-freshness link-check --files-from "$FILES_JSON"

      # ---------- Gate 3: lint-unsourced (ADVISORY) ----------
      # `if: always()` runs this even when gate 1/2 failed so reviewers see
      # advisory warnings alongside blocking errors. Advisory steps never fail
      # the workflow regardless of `if` condition.
      - name: 'Gate 3 of 5 — unsourced-claims lint (advisory)'
        if: always() && steps.changed.outputs.no_changes != '1'
        working-directory: base
        env:
          FILES_JSON: ${{ steps.changed.outputs.files_json }}
          DIFF_PATH: ${{ steps.changed.outputs.diff_path }}
        run: node dist/index.js knowledge-freshness lint-unsourced --files-from "$FILES_JSON" --diff "$DIFF_PATH"

      # ---------- Gate 4: anti-over-rewrite (BLOCKING for stable) ----------
      # Skip when the PR branch is NOT freshness-managed: human edits to stable
      # entries are gated by review, not by a 20% bot-applied heuristic.
      # Cron-opened PRs come from `knowledge-freshness/*` and DO get gated.
      #
      # F-005 / F-007: the override is a maintainer-applied label, NOT a PR
      # body marker. We extract labels from the PR event payload and pass them
      # explicitly via env var; the CLI never reads `gh pr view`.
      - name: 'Gate 4 of 5 — anti-over-rewrite (stable entries)'
        if: steps.changed.outputs.no_changes != '1' && startsWith(github.head_ref, 'knowledge-freshness/')
        working-directory: base
        env:
          PR_LABELS: ${{ join(github.event.pull_request.labels.*.name, ',') }}
          FILES_JSON: ${{ steps.changed.outputs.files_json }}
          DIFF_PATH: ${{ steps.changed.outputs.diff_path }}
        run: node dist/index.js knowledge-freshness anti-over-rewrite --pr-labels "$PR_LABELS" --files-from "$FILES_JSON" --diff "$DIFF_PATH"

      # ---------- Gate 5: Deep Guidance preserved (BLOCKING) ----------
      - name: 'Gate 5 of 5 — Deep Guidance heading preserved'
        if: steps.changed.outputs.no_changes != '1'
        working-directory: base
        env:
          FILES_JSON: ${{ steps.changed.outputs.files_json }}
        run: node dist/index.js knowledge-freshness deep-guidance-check --files-from "$FILES_JSON"
