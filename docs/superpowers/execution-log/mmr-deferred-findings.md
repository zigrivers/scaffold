# MMR Deferred Findings (P2/P3 after round-5 cap)

Findings deferred during autonomous rollout. Each entry includes enough context
to reproduce the original review verdict if you need to revisit. P0/P1 are
never deferred — they always block.

| Date | Plan | Task | PR | Severity | Location | Finding | Suggestion | Source channels | Notes |
|------|------|------|----|----------|----------|---------|------------|-----------------|-------|
| 2026-05-23T23:53:12Z | stopgap | 5 | 343 | P2 | src/observability/engine/llm-dispatcher.ts:39 | The 'terminateChild' constant is used in the 'timer' callback before its definition. While safe in Node.js because the callback runs on a later tick, it relies on temporal dead zone nuances and is less idiomatic than defining the helper first. | Move the 'terminateChild' definition above the 'timer' initialization, or use a hoisted function declaration (function terminateChild() { ... }). | gemini | Deferred per rounds 6+ policy; only P2 findings remained in MMR round 8. |
| 2026-05-23T23:53:12Z | stopgap | 5 | 343 | P2 | content/tools/review-code.md:404 | The shell script logic for finding normalization and hashing is duplicated from 'review-pr.md'. While explicitly noted as a stopgap, this duplication increases the risk of logic drift if bugs are fixed in one copy but not the other. | Consider extracting these shared bash helpers into a common script (e.g., 'scripts/review-helpers.sh') that both tool files can source, even if MMR v3.30 is expected soon. | gemini | Deferred per rounds 6+ policy; duplication is intentional for this markdown-only stopgap task. |
| 2026-05-23T23:53:12Z | stopgap | 5 | 343 | P2 | content/tools/review-code.md:468 | In '_review_normalize_description', the python script collapses all whitespace and newlines between code spans into a single space using ' '.join(). This might be too aggressive if line breaks within descriptions are significant for finding identity. | If line breaks are important, adjust the normalization to preserve newlines while still collapsing horizontal whitespace. | gemini | Deferred per rounds 6+ policy; only P2 findings remained in MMR round 8. |
| 2026-05-24T00:16:20Z | stopgap | 5 | 343 | P2 | src/observability/engine/llm-dispatcher.ts:100 | Redundant check for 'stdinError' in the 'close' event handler. If a stdin error occurs, 'failOnStdinError' is invoked, which sets 'resolved = true' and calls 'resolve()'. The 'close' handler exits early if 'resolved' is true, making these blocks (lines 92, 100-107) unreachable when 'stdinError' is set. | Remove the redundant 'stdinError' logic from the 'close' handler. The error is already handled and resolved by 'failOnStdinError'. | gemini | Deferred per rounds 6+ policy; only P2 findings remained after the latest P1 fix. |
| 2026-05-24T00:16:20Z | stopgap | 5 | 343 | P2 | src/observability/engine/llm-dispatcher.ts:39 | Temporal Dead Zone (TDZ) usage: 'terminateChild' is used in the 'setTimeout' callback before its 'const' definition. While functionally safe in Node.js because the callback runs on a later tick, it is non-idiomatic and potentially fragile. | Move the 'const terminateChild' definition above the 'const timer' initialization, or use a hoisted function declaration (e.g., 'function terminateChild() { ... }). | gemini | Deferred per rounds 6+ policy; restated after the latest P1 fix. |
| 2026-05-24T00:16:20Z | stopgap | 5 | 343 | P2 | content/tools/review-code.md:428 | Logic duplication: the bash/python helpers for finding normalization and hashing are duplicated from 'review-pr.md'. This increases the risk of logic drift between the two review tools. | Extract shared review bookkeeping helpers into a common script (e.g., 'scripts/review-helpers.sh') that both tool files can source. | gemini | Deferred per rounds 6+ policy; duplication is intentional for this markdown-only stopgap task. |
| 2026-05-24T00:54:07Z | stopgap | 5 | 343 | P2 | src/observability/engine/llm-dispatcher.ts:93 | Redundant logic: `stdinError` checks inside the `close` handler are unreachable. If a stdin error occurs, the `error` event synchronously calls `failOnStdinError`, which sets `resolved = true`. The `close` handler exits immediately if `resolved` is true. | Remove `stdinHint` and the `if (stdinError) { ... }` block from the `close` handler, as `failOnStdinError` already completely handles and resolves stdin errors. | gemini | Deferred per rounds 6+ policy; only P2 findings remained after fixing P1 hash and helper-order findings. |
| 2026-05-24T00:54:07Z | stopgap | 5 | 343 | P2 | src/observability/engine/llm-dispatcher.ts:100 | The close-handler stdinError handling is unreachable because the stdin error listener immediately calls failOnStdinError, sets resolved=true, and resolves the promise; the close handler exits before it can use stdinError. The added stderr/stdin hint composition for nonzero exits has the same issue for stdin errors. | Remove the stdinError state and the close-handler stdinError branches, or change the stdin error listener to only record the error if you intentionally want close to produce the final result. | codex | Deferred per rounds 6+ policy; same cleanup area as the Gemini P2 in this round. |
| 2026-05-24T02:53:53Z | stopgap | 7 | 345 | P2 | content/tools/review-code.md:366 | Inconsistent 'category' guidance between tools. content/tools/post-implementation-review.md:436 specifies an explicit enum (correctness\|edge-case\|security\|etc.), whereas review-code.md uses a generic placeholder description. | Update the JSON template in review-code.md to list the same stable categories (e.g., "correctness" \| "edge-case" \| "security" \| "test-coverage") to ensure consistent classification across all review channels. | gemini | Deferred per rounds 6+ policy; only P2 findings remained in MMR round 7. |
| 2026-05-24T02:53:53Z | stopgap | 7 | 345 | P2 | content/tools/review-code.md:713 | The 3-round limit hash includes 'description' and 'suggestion'. This is highly fragile as even trivial wording changes (jitter) from the LLM will generate a new hash, potentially bypassing the safety limit and causing unnecessary retry loops. | While noted as intentional for MMR T2-A parity, ensure the 'co-equal guard' for 'same underlying defect' is clearly prioritized in agent instructions to handle wording jitter that the strict hash misses. | gemini | Deferred per rounds 6+ policy; only P2 findings remained in MMR round 7. |
| 2026-05-24T03:15:57Z | stopgap | 7 | 345 | P2 | content/tools/review-code.md:366 | The 'location' field template uses 'file:line or section', which is inconsistent with the 'file:line' format mandated in 'content/tools/post-implementation-review.md:439' and described as a requirement for the strict validator on line 133. | Update the template value to 'relative/path/to/file.ts:42' for consistency across tools and to ensure compatibility with strict validation. | gemini | Deferred per rounds 6+ policy; only P2 findings remained after fixing P1 schema findings. |
| 2026-05-24T03:15:57Z | stopgap | 7 | 345 | P2 | docs/superpowers/execution-log/mmr-deferred-findings.md:17-18 | Round 7 findings are added to the deferred findings log even though the accompanying diff implements the suggested fixes for these specific findings. | If the findings are resolved in this PR, they should typically be removed from the deferred log or marked as resolved to keep the log's state accurate. | gemini | Deferred per rounds 6+ policy; finding is about deferred-log bookkeeping itself after later P1-overlapping edits. |
| 2026-05-24T03:15:57Z | stopgap | 7 | 345 | P2 | content/tools/post-implementation-review.md:482 | The documentation now states that `category` is required and that the strict `mmr reconcile` validator rejects findings missing it, but the actual MMR strict validator treats `category` as optional and only requires severity, location, and description. This makes the tool docs disagree with the CLI behavior and with `review-pr.md`. | Either update `packages/mmr/src/core/parser.ts` and its tests to make `category` required for strict external findings, or change these docs to say `category` is recommended/required by the wrapper prompt for stable identity but not rejected by `mmr reconcile` today. | codex | Deferred per rounds 6+ policy; only P2 findings remained after fixing P1 schema findings. |
| 2026-05-24T03:36:55Z | stopgap | 7 | 345 | P2 | content/tools/review-code.md:366 | Inconsistent placeholder format in the JSON template: 'severity' uses pipes between quoted strings, whereas 'category' uses pipes within a single quoted string. This inconsistency can confuse LLMs or lead to invalid JSON if copied literally. | Use the consistent 'value1\|value2\|value3' format inside a single set of quotes, matching the 'category' field and the templates in post-implementation-review.md. | gemini | Deferred per rounds 6+ policy; only P2 findings remained after fixing P1 location example. |
| 2026-05-24T03:36:55Z | stopgap | 7 | 345 | P2 | docs/superpowers/execution-log/mmr-deferred-findings.md:17-21 | New log entries use a future date (2026-05-24) relative to the current session (2026-05-23). Also, logging findings as 'Deferred' in the same PR that fixes them is confusing for audit history. | Sync log dates with the actual session date. If the findings are resolved in this PR, mark them as 'Resolved' or remove them from the 'Deferred' log to maintain accuracy. | gemini | Deferred per rounds 6+ policy; system date for this run is 2026-05-24, so timestamps are intentionally unchanged. |
| 2026-05-24T03:36:55Z | stopgap | 7 | 345 | P2 | content/tools/post-implementation-review.md:482 | The documentation says `mmr reconcile` strict validation rejects findings without `category`, but this diff only changes markdown prompts and does not show any validator change requiring `category`. If the CLI still accepts category-less findings, the runbook is inaccurate and may send users debugging the wrong failure mode. | Either update the MMR strict validator and tests to require `category`, or change this wording to say `category` is required by these review prompts for stable identity but not rejected by `mmr reconcile` today. | codex | Deferred per rounds 6+ policy; only P2 findings remained after fixing P1 location example. |
| 2026-05-24T12:47:19Z | v3.28 | 1 | 351 | P2 | packages/mmr/src/commands/review.ts:244 | Redundant check for chConfig.command inside the validChannels loop. Channels with missing commands are already filtered out during the auth check phase (lines 168-171) and never enter the validChannels array. | Remove the redundant if (!chConfig.command) checks in the dispatch loops at lines 244 and 269. | gemini | Deferred per rounds 6+ policy after MMR round 5; P1 inheritance findings are being fixed, but this P2 is intentionally not fixed post-cap. |
| 2026-05-24T12:47:19Z | v3.28 | 1 | 351 | P3 | packages/mmr/src/config/loader.ts:103 | Redundant use of structuredClone on parentResolved. parentResolved is already a fresh object returned from the recursive call to resolveChannelExtends, so cloning it again adds unnecessary overhead. | Remove the structuredClone call and use parentResolved directly. | gemini | Deferred per rounds 6+ policy after MMR round 5. |
| 2026-05-24T12:47:19Z | v3.28 | 1 | 351 | P3 | packages/mmr/src/config/loader.ts:114 | resolveExtendsAcrossChannels re-resolves the same parent channels multiple times for every child that extends them. While safe due to MAX_EXTENDS_DEPTH, it is inefficient for larger configurations. | Implement a simple cache (memoization) within resolveExtendsAcrossChannels to store already-resolved channels. | gemini | Deferred per rounds 6+ policy after MMR round 5. |
| 2026-05-24T13:10:55Z | v3.28 | 1 | 351 | P2 | packages/mmr/src/commands/review.ts:171 | Unreachable soft-fallback checks for missing channel commands remain in review.ts and config.ts even though loadConfig now validates that concrete channels define command. | Remove redundant command-presence checks from command execution paths after loader validation becomes authoritative. | gemini | Deferred per rounds 6+ policy after fixing the remaining round-6 P1 flag inheritance finding. |
| 2026-05-24T13:10:55Z | v3.28 | 1 | 351 | P2 | packages/mmr/src/config/loader.ts:102 | The depth guard uses stack.length >= MAX_EXTENDS_DEPTH, which rejects exactly four extends hops despite the error and tests describing the limit as depth greater than four. | Change the guard to reject only chains deeper than the configured maximum and add a boundary test for exactly four hops. | gemini | Deferred per rounds 6+ policy after fixing the remaining round-6 P1 flag inheritance finding. |
| 2026-05-24T13:33:00Z | v3.28 | 1 | 351 | P2 | packages/mmr/README.md:60 | The change adds user-facing channel inheritance fields (extends, abstract) and optional auth behavior, but the package README and changelog are not updated, leaving the new configuration surface undocumented. | Document extends/abstract usage in the MMR configuration section and add an Unreleased changelog entry describing the new inheritance and abstract-channel behavior. | codex | Deferred per rounds 6+ policy; MMR round 7 returned only P2 findings. |
| 2026-05-24T18:11:06Z | v3.28 | 5 | 356 | P2 | packages/mmr/src/commands/config.ts:85 | Multiple OSS example blocks are joined in the generated .mmr.yaml without intervening blank lines, resulting in a cramped and less readable configuration file. | Modify exampleBlocksFor to append a trailing newline to each block, for example: .map((runtime) => exampleBlockFor(runtime.id) + '\\n') | gemini | Deferred per rounds 6+ policy; MMR round 7 returned only P2/P3 findings. |
| 2026-05-24T18:11:06Z | v3.28 | 5 | 356 | P2 | packages/mmr/src/commands/config.ts:157 | This adds a user-facing `mmr config init --with-examples` option and changes the generated `.mmr.yaml`, but the diff does not update README or CHANGELOG as required by the repository instructions for user-facing behavior changes. | Add README documentation for the new config-init examples behavior/flag and add a CHANGELOG entry for the new OSS runtime example generation. | codex | Deferred per rounds 6+ policy; MMR round 7 returned only P2/P3 findings. |
| 2026-05-24T18:11:06Z | v3.28 | 5 | 356 | P3 | packages/mmr/src/commands/config.ts:165 | Using bracket notation args['with-examples'] is less idiomatic in TypeScript/yargs than the camelCased args.withExamples property provided by ArgumentsCamelCase. | Change to args.withExamples. | gemini | Deferred per rounds 6+ policy; MMR round 7 returned only P2/P3 findings. |
| 2026-05-24T18:11:06Z | v3.28 | 5 | 356 | P3 | packages/mmr/src/commands/config.ts:21 | The try/catch block in ossProbeResults is redundant because probeRuntime is designed to resolve all errors (including execution failures and timeouts) into a successful ProbeResult with detected: false. | Simplify the mapping logic by removing the try/catch block. | gemini | Deferred per rounds 6+ policy; MMR round 7 returned only P2/P3 findings. |
| 2026-05-24T21:25:26Z | v3.28 | 8 | 359 | P2 | packages/mmr/src/core/redact.ts:28 | NON_SECRET_ENV_NAME_KEYS only includes 'api_key_env', but 'api_token_env' is also commonly used in similar contexts and will currently be redacted at the top level, unlike 'api_key_env'. | Add 'api_token_env' to NON_SECRET_ENV_NAME_KEYS to ensure consistent behavior for environment variable name pointers. | gemini | Deferred per rounds 6+ policy; MMR round 9 returned only P2 findings. |
| 2026-05-24T22:59:24Z | v3.28 | 8 | 359 | P2 | packages/mmr/src/core/redact.ts:28 | NON_SECRET_ENV_NAME_KEYS only includes 'api_key_env'. Common variants used to store environment variable names (not secrets) like 'api_token_env' or 'auth_token_env' are missing, leading to unnecessary redaction of these pointers in configuration output. | Add 'api_token_env' and 'auth_token_env' to NON_SECRET_ENV_NAME_KEYS. | gemini,codex | Deferred per rounds 6+ policy; MMR round 15 returned only P2 findings. |
| 2026-05-25T00:26:09Z | v3.28 | 8 | 359 | P2 | packages/mmr/src/core/redact.ts:32 | isSecretKey will redact harmless MMR-specific keys like 'token-limit', 'token-usage', and 'session-dir' because they contain 'token' or 'session', which are in SECRET_KEY_PARTS. While isCommandSecretKey in config.ts explicitly exempts these for command strings, redactChannel (and thus redactRecord) does not, leading to over-redaction if these fields appear as top-level channel properties or inside env/headers objects. | Move the MMR-specific exemptions from isCommandSecretKey in config.ts to a shared location or include them in a configurable ignore list for isSecretKey to ensure consistent behavior across all redaction paths. | gemini | Deferred per rounds 6+ policy; MMR round 27 returned only P2/P3 findings. |
| 2026-05-25T00:26:09Z | v3.28 | 8 | 359 | P2 | packages/mmr/src/commands/config.ts:178 | Command redaction treats env-var pointer flags such as `--api-key-env OPENAI_API_KEY` as inline secrets because `isCommandSecretKey` forces `exemptEnvNameKeys: false`. That hides the whole command even though it contains only the env var name, conflicting with the `api_key_env` exemption used elsewhere. | Exempt command option names that are env-var pointers, such as normalized names ending in `-env`/`_env` or explicit `api-key-env`, `api-token-env`, and `auth-token-env`, while continuing to redact value-bearing env/header records. | codex | Deferred per rounds 6+ policy; MMR round 27 returned only P2/P3 findings. |
| 2026-05-25T00:26:09Z | v3.28 | 8 | 359 | P3 | packages/mmr/src/commands/config.ts:161 | The commandContainsInlineSecret function uses a simple regex-based tokenizer (match(/\"[^\"]*\"|'[^']*'|\\S+/g)). This may not correctly handle complex shell constructs like nested quotes or escaped delimiters, which could lead to false negatives in highly complex command strings. | If complex commands become common in configuration, consider using a dedicated shell-tokenizing library like 'shell-quote' for more robust parsing. | gemini | Deferred per rounds 6+ policy; MMR round 27 returned only P2/P3 findings. |
| 2026-05-25T00:38:24Z | v3.28 | 8 | 359 | P2 | packages/mmr/src/core/redact.ts:32 | isSecretKey does not strip quotes from the 'name' parameter, but the KEY_PART_RE regex does not include quotes as delimiters. This causes quoted keys (e.g., '\"API_KEY\"') to be split into parts like '\"api' and 'key\"', which fail to match secret keywords, potentially missing redactions in string-based list items. | Strip surrounding quotes from 'name' at the beginning of isSecretKey, or include quotes in the KEY_PART_RE delimiter regex. | gemini | Deferred per rounds 6+ policy; confirmation MMR returned only P2 findings. |
| 2026-05-25T00:38:24Z | v3.28 | 8 | 359 | P2 | packages/mmr/src/core/redact.ts:64 | redactKeyValueString uses a regex ([^:=\\s]+) for key extraction that captures surrounding quotes, which then causes isSecretKey to fail to identify the key as sensitive due to the quote-handling issue. | Refine the regex to handle optional quotes around the key and capture only the clean key name, or strip quotes before calling isSecretKey. | gemini | Deferred per rounds 6+ policy; confirmation MMR returned only P2 findings. |
| 2026-05-25T04:54:09Z | v3.28 | 10 | 365 | P2 | packages/mmr/src/commands/config.ts:264 | Potential redaction bypass in arrays. redactShowArray relies on isCommandSecretKey to identify secrets following specific flags. If a raw secret token appears in an array without a preceding flag, redactShowValue('', value) will not redact it because it lacks a 'secret' key name. | Perform value-based secret detection (e.g., using commandContainsInlineSecret) on all string elements in an array if they haven't been redacted by the flag-based logic. | gemini | Deferred per rounds 6+ policy; MMR round 5 returned only P2/P3 findings. |
| 2026-05-25T04:54:09Z | v3.28 | 10 | 365 | P2 | packages/mmr/src/commands/config.ts:275 | The commandContainsInlineSecret regex /(?:^|\s)--(?:...|sk-[a-z0-9]{20})\b/i incorrectly looks for 'sk-' tokens as flags (starting with --). Additionally, the second regex requires a leading space or equals sign, which may miss secrets at the start of a string or within array elements. | Use word boundaries \b instead of leading space/equals for token detection to ensure consistency across different placements: /\b(?:sk-[a-z0-9]{32}|[a-z0-9]{40})\b/. | gemini | Deferred per rounds 6+ policy; MMR round 5 returned only P2/P3 findings. |
| 2026-05-25T04:54:09Z | v3.28 | 10 | 365 | P3 | packages/mmr/src/commands/config.ts:189 | Redundant redaction call. redactShowChannel already handles command-like keys (command, check, recovery) via redactShowValue. The subsequent explicit call to redactDisplayCommand for display.command is unnecessary. | Remove the redundant block at lines 189-191. | gemini | Deferred per rounds 6+ policy; MMR round 5 returned only P2/P3 findings. |
| 2026-05-25T05:11:41Z | v3.28 | 10 | 365 | P3 | packages/mmr/src/commands/config.ts:339 | When multiple unexpected positional arguments are provided (e.g., 'mmr config init foo bar'), the error message reports the second argument ('bar') instead of the first unexpected argument ('foo') because `args.target` takes precedence in the nullish coalescing operator. | Change `args.target ?? args.name` to `args.name ?? args.target` to report the first unexpected argument encountered. | gemini | Deferred per rounds 6+ policy after MMR round 6; fixing the P1 array redaction finding only. |
| 2026-05-25T09:44:29Z | v3.28 | 13 | 368 | P2 | packages/mmr/src/commands/config.ts:178 | isNoRedact helper is fragile as it only checks 'no-redact' and ignores the 'redact' property it accepts. It introduces a hidden dependency on the middleware mutation to function correctly. | Either update isNoRedact to check both properties ('return args.redact === false \|\| args["no-redact"] === true') or revert to the previous inline logic which was more robust and self-contained. | gemini | Deferred per rounds 6+ policy after MMR round 5; only P2 findings remained. |
| 2026-05-25T09:44:29Z | v3.28 | 13 | 368 | P2 | packages/mmr/src/commands/config.ts:350 | The middleware used to sync 'redact' and 'no-redact' is redundant. Yargs automatically handles negation for boolean options (e.g., --no-redact sets redact: false). | Remove the middleware and use 'redact === false' as the single source of truth for redaction status. | gemini | Deferred per rounds 6+ policy after MMR round 5; only P2 findings remained. |
| 2026-05-25T09:44:29Z | v3.28 | 13 | 368 | P2 | packages/mmr/tests/commands/v3-28-flags.test.ts:6 | The 'parse' test helper replaces the command's actual handler with a mock that only captures arguments. This prevents testing of the handler logic itself (e.g., the call to configChannels with isNoRedact). | Add tests that verify the handler's behavior, possibly by mocking the underlying service calls rather than replacing the entire command handler. | gemini | Deferred per rounds 6+ policy after MMR round 5; only P2 findings remained. |
| 2026-05-25T09:57:34Z | v3.28 | 13 | 368 | P2 | packages/mmr/tests/commands/v3-28-flags.test.ts:6 | The new parse helper replaces each command's real handler, so the tests only verify yargs parsing and middleware output, not the behavior changed in configCommand's handler. A regression in the handler's noRedact calculation or configChannels invocation would not be caught. | Add a test that exercises the real config handler path and verifies --no-redact reaches configChannels/show behavior, using mocks for filesystem/config loading or service boundaries instead of replacing the command handler. | codex | Deferred per rounds 6+ policy after MMR round 6; only P2 findings remained. |
| 2026-05-25T10:13:06Z | v3.28 | 13 | 368 | P2 | packages/mmr/src/commands/config.ts:178 | isNoRedact accepts both redact and no-redact fields but only checks no-redact, so the handler now depends on middleware mutating args before it runs. If the handler is invoked directly or the middleware wiring changes, redact: false will no longer disable redaction. | Make isNoRedact self-contained by returning args.redact === false \|\| args['no-redact'] === true, or remove the helper and keep that logic inline in the handler. | codex | Deferred per rounds 6+ policy after MMR round 7; only P2 findings remained. |
| 2026-05-25T10:13:06Z | v3.28 | 13 | 368 | P2 | packages/mmr/tests/commands/v3-28-flags.test.ts:6 | The parse helper replaces each command's real handler, so these tests verify yargs parsing and middleware output but not that configCommand's handler passes noRedact through to configChannels/show behavior. | Add a test that exercises the real config handler path for config channels show with --no-redact, mocking config loading or the service boundary instead of replacing the command handler. | codex | Deferred per rounds 6+ policy after MMR round 7; only P2 findings remained. |
| 2026-05-25T14:58:00Z | v3.29 | 1 | 371 | P2 | packages/mmr/src/core/parser.ts:251 | Silent fallback for invalid severity captures in regex-findings parser can mask regex configuration errors. | If a severity capture index is provided but the captured string is not a valid Severity (P0-P3), consider treating it as a parsing error or providing a warning instead of silently falling back to the default severity. | gemini | Deferred per rounds 6+ policy after MMR round 5; only P2 findings remained. |
| 2026-05-25T14:58:00Z | v3.29 | 1 | 371 | P2 | packages/mmr/src/core/parser.ts:211 | unwrap-jsonpath uses strict JSON.parse which will fail if the output contains any non-JSON content, such as LLM-generated conversational preamble. | Consider pre-processing the raw string to extract the JSON block (e.g., finding the first '{' or '[') before passing it to JSON.parse if LLM output is a primary use case. | gemini | Deferred per rounds 6+ policy after MMR round 5; only P2 findings remained. |
| 2026-05-25T14:58:00Z | v3.29 | 1 | 371 | P2 | packages/mmr/src/core/parser.ts:227 | The custom jsonpathGet implementation is a minimal, non-standard subset. It fails on property names containing spaces, dots, or most special characters (except hyphens), as it lacks support for bracketed string keys like $['prop.name']. | Explicitly document the supported JSONPath subset or use a lightweight standard library to avoid surprising users who expect full JSONPath support. | gemini | Deferred per rounds 6+ policy after MMR round 5; only P2 findings remained. |
| 2026-05-25T15:12:00Z | v3.29 | 1 | 371 | P2 | packages/mmr/src/config/schema.ts:54 | Regex flags validation allows duplicate flags (e.g., 'gg') and mutually exclusive flags (e.g., 'uv'), which will cause the RegExp constructor to throw at runtime. | Add a .refine() to the flags schema to validate flag uniqueness and compatibility (e.g., checking that new Set(flags).size === flags.length and that 'u' and 'v' are not both present). | gemini | Deferred per rounds 6+ policy after confirmation MMR; only P2 findings remained. |
| 2026-05-25T15:12:00Z | v3.29 | 1 | 371 | P2 | packages/mmr/src/config/schema.ts:44 | The flags schema accepts duplicate or incompatible RegExp flags such as "gg" or "uv", and also accepts "v" despite the package supporting Node >=18 where that flag may be unavailable. These configs pass validation but later fail at parse time when new RegExp() is constructed. | Validate flags by attempting new RegExp('', flags) in a zod refinement, or restrict the allowed flags to the runtime-supported set and reject duplicates/incompatible combinations during config parsing. | codex | Deferred per rounds 6+ policy after confirmation MMR; only P2 findings remained. |
| 2026-05-25T21:55:43Z | v3.29 | 8 | 381 | P2 | packages/mmr/README.md:97 | The description of `wrap` as a 'schema key' is confusing since it is actually the field name in the configuration object, and its value is the JSONPath selector. | Reword to: "`wrap` is the JSONPath selector used to extract the content from the response envelope." | gemini | Deferred per rounds 6+ policy after MMR round 5; fixing P1 regex YAML escaping only. |
| 2026-05-25T21:55:43Z | v3.29 | 8 | 381 | P2 | packages/mmr/README.md:79 | The example references `scripts/ollama-openai-chat.sh` which is not present in the repository's `scripts/` directory. | Either add the script to the repository or provide its contents as a code block in the README to make the example runnable for users. | gemini | Deferred per rounds 6+ policy after MMR round 5; fixing P1 regex YAML escaping only. |
| 2026-05-26T02:08:07Z | v3.29 | 12 | 384 | P2 | packages/mmr/src/core/compensator.ts:24 | The 'focusPrompt' field in 'CompensatingChannel' is redundant; it's still calculated and returned by 'getCompensatingChannels' but ignored by 'dispatchCompensatingPasses', which now uses 'resolveCompensatorFocus'. | Remove 'focusPrompt' from the 'CompensatingChannel' interface and 'getCompensatingChannels' implementation to clean up dead data/logic. | gemini | Deferred per rounds 6+ policy after post-cap MMR confirmation; no P0/P1 remained. |
| 2026-05-26T02:08:07Z | v3.29 | 12 | 384 | P2 | packages/mmr/src/core/compensator.ts:98 | The fallback focus prompt in 'resolveCompensatorFocus' differs slightly in wording from the one still hardcoded in 'getCompensatingChannels', which might lead to confusion if the redundant field is ever reused. | Consolidate the fallback logic into 'resolveCompensatorFocus' and remove the old string from 'getCompensatingChannels'. | gemini | Deferred per rounds 6+ policy after post-cap MMR confirmation; no P0/P1 remained. |
